{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:bitcoinuniverse:block-20:mint-hash-profile:v1",
  "title": "Bitcoin Universe BLOCK-20 canonical mint-hash profile",
  "description": "Recommended interoperability validation for a parsed BLOCK-20 mint payload. This schema validates canonical display syntax only. It does not prove active-chain membership, freshness, the target network, reorganization safety, or reader acceptance.",
  "type": "object",
  "additionalProperties": false,
  "required": ["p", "op", "tick", "amt", "hash"],
  "properties": {
    "p": { "const": "block-20" },
    "op": { "const": "mint" },
    "tick": { "type": "string", "minLength": 1 },
    "amt": { "type": "string" },
    "hash": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "32-byte Bitcoin block ID in canonical lowercase hexadecimal display form, without whitespace or a 0x prefix."
    }
  }
}
